# Add 4 Demos (Laguku iframe + 3 TaxAI re-implement) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tambah 4 demo baru ke portfolio (9 total): Laguku sebagai iframe live site, dan 3 TaxAI (wizard/chat/talk) sebagai re-implement sintetik dengan mock fixtures.

**Architecture:** Append-only. Tidak mengubah 5 demo existing. Tambah 4 entry ke `DEMOS` registry, 4 theme file, 4 demo folder (1 iframe, 3 dengan screens + mocks). Brand logo dari produksi di-copy ke `public/assets/images/logos/`.

**Tech Stack:** React 19 + Vite + TypeScript + Tailwind v4 + shadcn primitives (`_shared/`). Untuk voice waveform di TaxAI Talk: SVG/CSS animation (no new deps). Hash router existing.

---

## File Structure

### New files

```
public/assets/images/logos/
├── laguku.png                       # copied from laguin.id/apps/web/public/logo.png
└── taxai.png                        # copied from tax-ai-wizard-web-70/public/lovable-uploads/logo yosr.png

src/demos/_shared/themes/
├── laguku.ts
├── taxai-wizard.ts
├── taxai-chat.ts
└── taxai-talk.ts

src/demos/laguku/
├── index.tsx
├── routes.tsx
└── README.md

src/demos/taxai-wizard/
├── index.tsx
├── routes.tsx
├── mocks.ts
├── README.md
└── screens/{Register,Plans,Checkout,Dashboard}.tsx

src/demos/taxai-chat/
├── index.tsx
├── routes.tsx
├── mocks.ts
├── README.md
└── screens/{Inbox,Conversation,Settings}.tsx

src/demos/taxai-talk/
├── index.tsx
├── routes.tsx
├── mocks.ts
├── README.md
└── screens/{VoiceSession,Transcript,Settings}.tsx
```

### Modified files

```
src/demos/_index.ts                  # +4 DemoId union members, +4 DEMOS entries
src/demos/router.tsx                 # +4 imports, +4 DEMO_COMPONENTS, MobileViewportNotice override
src/demos/_shared/theme.ts           # +4 theme imports, +4 THEMES entries
src/demos/_shared/Brand.tsx          # +4 LOGOS entries
```

---

## Task 1: Copy logo assets

**Files:**
- Create: `public/assets/images/logos/laguku.png`
- Create: `public/assets/images/logos/taxai.png`

- [ ] **Step 1: Copy Laguku logo**

```bash
cp /Users/yanuar/Documents/laguin.id/apps/web/public/logo.png \
   /Users/yanuar/Documents/abdurrahmanfirdaus.com/public/assets/images/logos/laguku.png
ls -la /Users/yanuar/Documents/abdurrahmanfirdaus.com/public/assets/images/logos/laguku.png
```

Expected: file exists, ~270KB PNG (540×200 RGBA).

- [ ] **Step 2: Download Taxai logo via curl**

```bash
curl -fsSL "https://raw.githubusercontent.com/daws11/tax-ai-wizard-web-70/main/public/lovable-uploads/logo%20yosr.png" \
  -o /Users/yanuar/Documents/abdurrahmanfirdaus.com/public/assets/images/logos/taxai.png
ls -la /Users/yanuar/Documents/abdurrahmanfirdaus.com/public/assets/images/logos/taxai.png
```

Expected: file exists (size depends on source).

- [ ] **Step 3: Commit**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
git add public/assets/images/logos/laguku.png public/assets/images/logos/taxai.png
git commit -m "feat(demos): add laguku + taxai logo assets"
```

---

## Task 2: Create 4 theme files

**Files:**
- Create: `src/demos/_shared/themes/laguku.ts`
- Create: `src/demos/_shared/themes/taxai-wizard.ts`
- Create: `src/demos/_shared/themes/taxai-chat.ts`
- Create: `src/demos/_shared/themes/taxai-talk.ts`

Reference existing themes (e.g. `themes/invenflow.ts`) for shape. Each exports a `DemoTheme` object.

- [ ] **Step 1: Create `laguku.ts`**

Write to `src/demos/_shared/themes/laguku.ts`:

```ts
// src/demos/_shared/themes/laguku.ts
import type { DemoTheme } from "../theme";

export const lagukuTheme: DemoTheme = {
  id: "laguku",
  brand: { name: "Laguku", monogram: "LK", surface: "light" },
  tokens: {
    "--bg": "#FFFFFF",
    "--surface": "#FFF1F2",
    "--fg": "#1F2937",
    "--muted": "#6B7280",
    "--border": "#FECDD3",
    "--accent": "#E11D48",
    "--accent-fg": "#FFFFFF",
    "--warn": "#F59E0B",
    "--ok": "#10B981",
    "--bad": "#EF4444",
  },
  shell: { sidebarWidth: 240, sidebarCollapsedWidth: 64, topBarHeight: 56, density: "comfortable" },
  font: { sans: "Inter, system-ui, sans-serif" },
};
```

- [ ] **Step 2: Create `taxai-wizard.ts`**

Write to `src/demos/_shared/themes/taxai-wizard.ts`:

```ts
// src/demos/_shared/themes/taxai-wizard.ts
import type { DemoTheme } from "../theme";

export const taxaiWizardTheme: DemoTheme = {
  id: "taxai-wizard",
  brand: { name: "TaxAI Wizard", monogram: "TW", surface: "light" },
  tokens: {
    "--bg": "#FFFFFF",
    "--surface": "#F0FDFA",
    "--fg": "#0F172A",
    "--muted": "#64748B",
    "--border": "#CCFBF1",
    "--accent": "#0EA5A4",
    "--accent-fg": "#FFFFFF",
    "--warn": "#F59E0B",
    "--ok": "#10B981",
    "--bad": "#EF4444",
  },
  shell: { sidebarWidth: 240, sidebarCollapsedWidth: 64, topBarHeight: 56, density: "comfortable" },
  font: { sans: "Inter, system-ui, sans-serif" },
};
```

- [ ] **Step 3: Create `taxai-chat.ts`**

Write to `src/demos/_shared/themes/taxai-chat.ts`:

```ts
// src/demos/_shared/themes/taxai-chat.ts
import type { DemoTheme } from "../theme";

export const taxaiChatTheme: DemoTheme = {
  id: "taxai-chat",
  brand: { name: "TaxAI Chat", monogram: "TC", surface: "light" },
  tokens: {
    "--bg": "#FFFFFF",
    "--surface": "#EEF2FF",
    "--fg": "#0F172A",
    "--muted": "#64748B",
    "--border": "#C7D2FE",
    "--accent": "#4F46E5",
    "--accent-fg": "#FFFFFF",
    "--warn": "#F59E0B",
    "--ok": "#10B981",
    "--bad": "#EF4444",
  },
  shell: { sidebarWidth: 280, sidebarCollapsedWidth: 240, topBarHeight: 56, density: "comfortable" },
  font: { sans: "Inter, system-ui, sans-serif" },
};
```

- [ ] **Step 4: Create `taxai-talk.ts`**

Write to `src/demos/_shared/themes/taxai-talk.ts`:

```ts
// src/demos/_shared/themes/taxai-talk.ts
import type { DemoTheme } from "../theme";

export const taxaiTalkTheme: DemoTheme = {
  id: "taxai-talk",
  brand: { name: "TaxAI Talk", monogram: "TT", surface: "dark" },
  tokens: {
    "--bg": "#0B0B14",
    "--surface": "#15152B",
    "--fg": "#F5F3FF",
    "--muted": "#A78BFA",
    "--border": "#312E81",
    "--accent": "#7C3AED",
    "--accent-fg": "#FFFFFF",
    "--warn": "#FBBF24",
    "--ok": "#34D399",
    "--bad": "#F87171",
  },
  shell: { sidebarWidth: 240, sidebarCollapsedWidth: 64, topBarHeight: 56, density: "comfortable" },
  font: { sans: "Inter, system-ui, sans-serif" },
};
```

- [ ] **Step 5: Commit**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
git add src/demos/_shared/themes/laguku.ts src/demos/_shared/themes/taxai-wizard.ts src/demos/_shared/themes/taxai-chat.ts src/demos/_shared/themes/taxai-talk.ts
git commit -m "feat(demos): add 4 theme files (laguku, taxai-{wizard,chat,talk})"
```

---

## Task 3: Wire theme registry + DemoId union

**Files:**
- Modify: `src/demos/_shared/theme.ts`
- Modify: `src/demos/_index.ts`

- [ ] **Step 1: Update `theme.ts` — extend DemoTheme.id union and THEMES map**

Edit `src/demos/_shared/theme.ts`:

1. Change `id` union (line 10) from:
```ts
id: "invenflow" | "invoice-sense" | "channelflow" | "kitchen-fresh" | "people-culture";
```
to:
```ts
id: "invenflow" | "invoice-sense" | "channelflow" | "kitchen-fresh" | "people-culture" | "laguku" | "taxai-wizard" | "taxai-chat" | "taxai-talk";
```

2. After existing theme imports (line 46), add:
```ts
import { lagukuTheme } from "./themes/laguku";
import { taxaiWizardTheme } from "./themes/taxai-wizard";
import { taxaiChatTheme } from "./themes/taxai-chat";
import { taxaiTalkTheme } from "./themes/taxai-talk";
```

3. In `THEMES` object (line 48), add 4 entries:
```ts
laguku: lagukuTheme,
"taxai-wizard": taxaiWizardTheme,
"taxai-chat": taxaiChatTheme,
"taxai-talk": taxaiTalkTheme,
```

- [ ] **Step 2: Update `_index.ts` — extend DemoId union + add 4 DEMOS entries**

Edit `src/demos/_index.ts`:

1. Change `DemoId` union (line 8-13) from:
```ts
export type DemoId =
  | "invenflow"
  | "invoice-sense"
  | "channelflow"
  | "kitchen-fresh"
  | "people-culture";
```
to:
```ts
export type DemoId =
  | "invenflow"
  | "invoice-sense"
  | "channelflow"
  | "kitchen-fresh"
  | "people-culture"
  | "laguku"
  | "taxai-wizard"
  | "taxai-chat"
  | "taxai-talk";
```

2. After the 5 existing `DEMOS` entries (after line 72, before `getDemoById` function), append 4 new entries:

```ts
  {
    id: "laguku",
    title: "Laguku",
    division: "AI · Music orchestration",
    blurb: "GPT writes the lyrics, Suno composes the track — delivered via WhatsApp.",
    route: "/demos/laguku",
    status: "live",
    theme: THEMES.laguku,
  },
  {
    id: "taxai-wizard",
    title: "TaxAI Wizard",
    division: "AI · Stripe subscription",
    blurb: "UAE tax intelligence behind a Stripe subscription — Free Trial to Yearly.",
    route: "/demos/taxai-wizard",
    status: "live",
    theme: THEMES["taxai-wizard"],
  },
  {
    id: "taxai-chat",
    title: "TaxAI Chat",
    division: "AI · Document Q&A",
    blurb: "Ask the UAE tax code — GPT answers with document citations.",
    route: "/demos/taxai-chat",
    status: "live",
    theme: THEMES["taxai-chat"],
  },
  {
    id: "taxai-talk",
    title: "TaxAI Talk",
    division: "AI · Voice pipeline",
    blurb: "GPT reasons, ElevenLabs speaks — tax answers in multiple languages.",
    route: "/demos/taxai-talk",
    status: "live",
    theme: THEMES["taxai-talk"],
  },
```

- [ ] **Step 3: Run typecheck**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
npm run typecheck 2>&1 | tail -20
```

Expected: no errors (router.tsx and Brand.tsx still need updating — those will fail because the 4 demo components don't exist yet, but the registry should compile).

If typecheck fails on missing components, that's expected — continue to Task 4.

- [ ] **Step 4: Commit**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
git add src/demos/_shared/theme.ts src/demos/_index.ts
git commit -m "feat(demos): register 4 new demos in DEMOS array + theme map"
```

---

## Task 4: Wire router + Brand

**Files:**
- Modify: `src/demos/_shared/Brand.tsx`
- Modify: `src/demos/router.tsx`

- [ ] **Step 1: Update `Brand.tsx` — add 4 LOGOS entries**

Edit `src/demos/_shared/Brand.tsx`:

1. Change `DemoTheme["id"]` reference inside `LogoAsset` lookup. Update `LOGOS` constant (line 30-36) by adding 4 entries:

```ts
const LOGOS: Record<string, LogoAsset> = {
  "invoice-sense": { src: "", kind: "tile", backgroundColor: "var(--accent)", color: "var(--accent-fg)" },
  invenflow: { src: "/assets/images/logos/invenflow.svg", kind: "image" },
  channelflow: { src: "/assets/images/logos/channelflow.png", kind: "image" },
  "kitchen-fresh": { src: "/assets/images/logos/kitchen-fresh.svg", kind: "image" },
  "people-culture": { src: "/assets/images/logos/people-culture.png", kind: "image" },
  laguku: { src: "/assets/images/logos/laguku.png", kind: "image" },
  "taxai-wizard": { src: "/assets/images/logos/taxai.png", kind: "image" },
  "taxai-chat": { src: "/assets/images/logos/taxai.png", kind: "image" },
  "taxai-talk": { src: "/assets/images/logos/taxai.png", kind: "image" },
};
```

- [ ] **Step 2: Update `router.tsx` — add imports, DEMO_COMPONENTS, MobileViewportNotice override**

Edit `src/demos/router.tsx`:

1. After existing imports (line 19-24), add:
```ts
import { Laguku } from "./laguku";
import { TaxaiWizard } from "./taxai-wizard";
import { TaxaiChat } from "./taxai-chat";
import { TaxaiTalk } from "./taxai-talk";
```

2. In `DEMO_COMPONENTS` (line 71-77), add 4 entries:
```ts
const DEMO_COMPONENTS: Record<DemoId, ComponentType<{ sub: string | null; theme: DemoTheme }>> = {
  invenflow: Invenflow,
  "invoice-sense": InvoiceSense,
  channelflow: Channelflow,
  "kitchen-fresh": KitchenFresh,
  "people-culture": PeopleCulture,
  laguku: Laguku,
  "taxai-wizard": TaxaiWizard,
  "taxai-chat": TaxaiChat,
  "taxai-talk": TaxaiTalk,
};
```

3. In `DemoRouter` function (line 79), update the return block (line 96-101) to skip `MobileViewportNotice` for Laguku:

```tsx
const Demo = DEMO_COMPONENTS[route.id];
const isLaguku = route.id === "laguku";
return (
  <>
    <Demo sub={route.sub} theme={meta.theme} />
    {!isLaguku && <MobileViewportNotice />}
  </>
);
```

- [ ] **Step 3: Run typecheck**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
npm run typecheck 2>&1 | tail -20
```

Expected: 4 errors of form `Cannot find module './laguku'` etc. These are expected until Tasks 5-8 create those modules. **Continue to next task** — typecheck will pass after all 4 demo modules exist.

- [ ] **Step 4: Commit**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
git add src/demos/_shared/Brand.tsx src/demos/router.tsx
git commit -m "feat(demos): wire router + brand logos for 4 new demos"
```

---

## Task 5: Build Laguku (iframe)

**Files:**
- Create: `src/demos/laguku/routes.tsx`
- Create: `src/demos/laguku/index.tsx`
- Create: `src/demos/laguku/README.md`

- [ ] **Step 1: Create `routes.tsx`**

Write to `src/demos/laguku/routes.tsx`:

```ts
// src/demos/laguku/routes.tsx
//
// Laguku demo has no sub-screens — it's a single iframe embedding the live
// laguku.co site. The router passes `sub` but we ignore it; if a sub route
// is requested we simply render the same iframe.

export type LagukuScreen = "live";

export const LAGUKU_SCREENS: { id: LagukuScreen; label: string }[] = [
  { id: "live", label: "Live" },
];

export function getScreenLabel(sub: string | null, fallback: LagukuScreen): LagukuScreen {
  return fallback;
}
```

- [ ] **Step 2: Create `index.tsx`**

Write to `src/demos/laguku/index.tsx`:

```tsx
// src/demos/laguku/index.tsx
//
// Laguku demo renders the live laguku.co site inside an iframe, wrapped in
// the shared Shell for portfolio chrome (top bar + brand tile). No sub-routes,
// no fixtures — the live site IS the demo.

import { Shell } from "@/demos/_shared/Shell";
import { useTheme } from "@/demos/_shared/useTheme";
import type { DemoTheme } from "@/demos/_shared/theme";

export function Laguku({ theme, sub: _sub }: { theme: DemoTheme; sub: string | null }) {
  useTheme(theme.id);

  return (
    <Shell theme={theme}>
      <iframe
        src="https://laguku.co"
        title="Laguku — live site"
        className="h-[calc(100dvh-var(--topbar-h,56px))] w-full border-0 bg-white"
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
        referrerPolicy="strict-origin-when-cross-origin"
      />
    </Shell>
  );
}
```

- [ ] **Step 3: Create `README.md`**

Write to `src/demos/laguku/README.md`:

```markdown
# Laguku

This demo embeds the live **laguku.co** website inside the portfolio shell.
The iframe loads the production site at full height; portfolio chrome (top
bar, brand tile) wraps it. No sub-screens, no fixtures, no re-implementation
— the live site is the demo. Refresh / interaction state belongs to
laguku.co, not to this wrapper.

If laguku.co sets `X-Frame-Options: DENY` in the future, replace the iframe
with an "External site refuses embed" message and a link to open it
directly.
```

- [ ] **Step 4: Run typecheck for Laguku alone**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
npm run typecheck 2>&1 | grep -E "(laguku|error TS)" | head -10
```

Expected: laguku.tsx errors gone. Other 3 (taxai-*) still failing until Tasks 6-8.

- [ ] **Step 5: Commit**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
git add src/demos/laguku/
git commit -m "feat(demos): add Laguku demo (iframe live site)"
```

---

## Task 6: Build TaxAI Wizard

**Files:**
- Create: `src/demos/taxai-wizard/routes.tsx`
- Create: `src/demos/taxai-wizard/mocks.ts`
- Create: `src/demos/taxai-wizard/screens/Register.tsx`
- Create: `src/demos/taxai-wizard/screens/Plans.tsx`
- Create: `src/demos/taxai-wizard/screens/Checkout.tsx`
- Create: `src/demos/taxai-wizard/screens/Dashboard.tsx`
- Create: `src/demos/taxai-wizard/index.tsx`
- Create: `src/demos/taxai-wizard/README.md`

- [ ] **Step 1: Create `routes.tsx`**

Write to `src/demos/taxai-wizard/routes.tsx`:

```ts
// src/demos/taxai-wizard/routes.tsx
//
// TaxAI Wizard sub-routes: register (default) → plans → checkout → dashboard.

export type TaxaiWizardScreen = "register" | "plans" | "checkout" | "dashboard";

export const TAXAI_WIZARD_SCREENS: { id: TaxaiWizardScreen; label: string }[] = [
  { id: "register", label: "Register" },
  { id: "plans", label: "Plans" },
  { id: "checkout", label: "Checkout" },
  { id: "dashboard", label: "Dashboard" },
];

export function getScreenLabel(
  sub: string | null,
  fallback: TaxaiWizardScreen,
): TaxaiWizardScreen {
  if (!sub) return fallback;
  const found = TAXAI_WIZARD_SCREENS.find((s) => s.id === sub);
  return (found?.id ?? fallback) as TaxaiWizardScreen;
}
```

- [ ] **Step 2: Create `mocks.ts`**

Write to `src/demos/taxai-wizard/mocks.ts`:

```ts
// src/demos/taxai-wizard/mocks.ts
//
// Synthetic fixtures for TaxAI Wizard. All names, emails, plan data are
// invented for the demo. No production code or schemas reused.

export interface Plan {
  id: "trial" | "monthly" | "quarterly" | "yearly";
  name: string;
  priceUsd: number;
  interval: "14 days" | "month" | "quarter" | "year";
  messageQuota: number;
  features: string[];
  highlighted?: boolean;
}

export interface Subscription {
  planId: Plan["id"];
  startedAt: string;
  expiresAt: string;
  messagesUsed: number;
}

export interface User {
  name: string;
  email: string;
  jobTitle: string;
  country: string;
}

export const PLANS: Plan[] = [
  {
    id: "trial",
    name: "Free Trial",
    priceUsd: 0,
    interval: "14 days",
    messageQuota: 50,
    features: ["50 messages", "GPT-4o tax Q&A", "Document upload (3 MB)", "Email support"],
  },
  {
    id: "monthly",
    name: "Monthly",
    priceUsd: 99,
    interval: "month",
    messageQuota: 100,
    features: ["100 messages / month", "GPT-4o tax Q&A", "Document upload (10 MB)", "Priority support"],
  },
  {
    id: "quarterly",
    name: "Quarterly",
    priceUsd: 250,
    interval: "quarter",
    messageQuota: 300,
    features: ["300 messages / quarter", "GPT-4o tax Q&A", "Document upload (25 MB)", "Priority support", "Tax calendar reminders"],
    highlighted: true,
  },
  {
    id: "yearly",
    name: "Yearly",
    priceUsd: 899,
    interval: "year",
    messageQuota: 1200,
    features: ["1,200 messages / year", "GPT-4o tax Q&A", "Document upload (unlimited)", "Dedicated CSM", "Custom tax prompts"],
  },
];

export const SAMPLE_USER: User = {
  name: "Sara Al-Mansouri",
  email: "sara.mansouri@example.ae",
  jobTitle: "Freelance Tax Consultant",
  country: "United Arab Emirates",
};

export const SAMPLE_SUBSCRIPTION: Subscription = {
  planId: "quarterly",
  startedAt: "2026-07-15",
  expiresAt: "2026-10-15",
  messagesUsed: 87,
};
```

- [ ] **Step 3: Create `screens/Register.tsx`**

Write to `src/demos/taxai-wizard/screens/Register.tsx`:

```tsx
// src/demos/taxai-wizard/screens/Register.tsx
//
// Registration step — multi-field form. Mock state only; submit jumps to
// /plans via setDemoHash.

import { useState } from "react";
import { useTheme } from "@/demos/_shared/useTheme";
import { Button } from "@/demos/_shared/Button";
import { Field } from "@/demos/_shared/Field";
import { setDemoHash } from "@/demos/router";

export function Register() {
  useTheme("taxai-wizard");
  const [form, setForm] = useState({ name: "", email: "", password: "", jobTitle: "" });

  const onSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    setDemoHash("taxai-wizard", "plans");
  };

  return (
    <div className="mx-auto max-w-md px-6 py-10">
      <h2 className="text-2xl font-semibold tracking-tight">Create your TaxAI account</h2>
      <p className="mt-1 text-sm text-[color:var(--muted)]">Start your 14-day Free Trial. No credit card required.</p>

      <form onSubmit={onSubmit} className="mt-6 space-y-4">
        <Field label="Full name" value={form.name} onChange={(v) => setForm({ ...form, name: v })} placeholder="Sara Al-Mansouri" />
        <Field label="Work email" type="email" value={form.email} onChange={(v) => setForm({ ...form, email: v })} placeholder="you@company.ae" />
        <Field label="Password" type="password" value={form.password} onChange={(v) => setForm({ ...form, password: v })} placeholder="At least 8 characters" />
        <Field label="Job title" value={form.jobTitle} onChange={(v) => setForm({ ...form, jobTitle: v })} placeholder="Tax consultant" />

        <Button type="submit" variant="primary" className="mt-2 w-full">
          Continue to plans →
        </Button>
      </form>

      <p className="mt-4 text-center text-xs text-[color:var(--muted)]">
        By continuing you agree to the Terms of Service and Privacy Policy.
      </p>
    </div>
  );
}
```

- [ ] **Step 4: Create `screens/Plans.tsx`**

Write to `src/demos/taxai-wizard/screens/Plans.tsx`:

```tsx
// src/demos/taxai-wizard/screens/Plans.tsx
//
// 4 tier cards (Trial / Monthly / Quarterly / Yearly). Highlighted tier
// ("Quarterly") renders a "Most popular" ribbon. Click → /checkout with
// selected plan id (encoded in URL hash).

import { Check } from "lucide-react";
import { useTheme } from "@/demos/_shared/useTheme";
import { Button } from "@/demos/_shared/Button";
import { cn } from "@/lib/utils";
import { PLANS } from "../mocks";
import { setDemoHash } from "@/demos/router";

export function Plans() {
  useTheme("taxai-wizard");

  return (
    <div className="mx-auto max-w-6xl px-6 py-10">
      <div className="text-center">
        <h2 className="text-2xl font-semibold tracking-tight">Choose your plan</h2>
        <p className="mt-1 text-sm text-[color:var(--muted)]">All plans include GPT-4o tax Q&A and document upload.</p>
      </div>

      <div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
        {PLANS.map((plan) => (
          <div
            key={plan.id}
            className={cn(
              "relative flex flex-col rounded-lg border bg-[color:var(--surface)] p-5 transition-colors",
              plan.highlighted ? "border-[color:var(--accent)] shadow-sm" : "border-[color:var(--border)]",
            )}
            style={plan.highlighted ? { boxShadow: "0 0 0 1px var(--accent)" } : undefined}
          >
            {plan.highlighted && (
              <span className="absolute -top-2 right-3 rounded-full bg-[color:var(--accent)] px-2 py-0.5 text-[10px] font-semibold uppercase tracking-wider text-[color:var(--accent-fg)]">
                Most popular
              </span>
            )}
            <h3 className="text-lg font-semibold">{plan.name}</h3>
            <div className="mt-2 flex items-baseline gap-1">
              <span className="text-3xl font-semibold tracking-tight">${plan.priceUsd}</span>
              <span className="text-xs text-[color:var(--muted)]">/ {plan.interval}</span>
            </div>
            <p className="mt-1 text-xs text-[color:var(--muted)]">{plan.messageQuota.toLocaleString()} messages</p>

            <ul className="mt-4 flex-1 space-y-1.5">
              {plan.features.map((f) => (
                <li key={f} className="flex items-start gap-2 text-xs">
                  <Check className="mt-0.5 h-3.5 w-3.5 shrink-0 text-[color:var(--ok)]" />
                  <span>{f}</span>
                </li>
              ))}
            </ul>

            <Button
              type="button"
              variant={plan.highlighted ? "primary" : "secondary"}
              className="mt-5 w-full"
              onClick={() => setDemoHash("taxai-wizard", "checkout")}
            >
              {plan.priceUsd === 0 ? "Start free trial" : "Subscribe"}
            </Button>
          </div>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 5: Create `screens/Checkout.tsx`**

Write to `src/demos/taxai-wizard/screens/Checkout.tsx`:

```tsx
// src/demos/taxai-wizard/screens/Checkout.tsx
//
// Stripe Elements mock — shows a card form preview and an order summary.
// "Pay" button → /dashboard. No real Stripe integration.

import { CreditCard, Lock } from "lucide-react";
import { useTheme } from "@/demos/_shared/useTheme";
import { Button } from "@/demos/_shared/Button";
import { Field } from "@/demos/_shared/Field";
import { PLANS } from "../mocks";
import { setDemoHash } from "@/demos/router";

export function Checkout() {
  useTheme("taxai-wizard");
  const plan = PLANS.find((p) => p.id === "quarterly")!;

  const onPay = () => setDemoHash("taxai-wizard", "dashboard");

  return (
    <div className="mx-auto grid max-w-4xl gap-6 px-6 py-10 lg:grid-cols-[1fr_360px]">
      {/* Payment form (mock Stripe Elements) */}
      <div className="rounded-lg border border-[color:var(--border)] bg-[color:var(--surface)] p-6">
        <h3 className="flex items-center gap-2 text-base font-semibold">
          <CreditCard className="h-4 w-4" /> Payment details
        </h3>
        <p className="mt-1 text-xs text-[color:var(--muted)]">Powered by Stripe. Your card is encrypted on submit.</p>

        <div className="mt-6 space-y-4">
          <Field label="Card number" value="4242 4242 4242 4242" onChange={() => {}} placeholder="1234 1234 1234 1234" />
          <div className="grid grid-cols-2 gap-3">
            <Field label="Expiry" value="12 / 28" onChange={() => {}} placeholder="MM / YY" />
            <Field label="CVC" value="123" onChange={() => {}} placeholder="123" />
          </div>
          <Field label="Name on card" value="Sara Al-Mansouri" onChange={() => {}} placeholder="Full name" />
        </div>

        <Button type="button" variant="primary" className="mt-6 w-full" onClick={onPay}>
          <Lock className="mr-2 h-4 w-4" /> Pay ${plan.priceUsd}.00
        </Button>
      </div>

      {/* Order summary */}
      <div className="rounded-lg border border-[color:var(--border)] bg-[color:var(--surface)] p-6">
        <h3 className="text-sm font-semibold uppercase tracking-wider text-[color:var(--muted)]">Order summary</h3>
        <div className="mt-4 space-y-2">
          <div className="flex items-baseline justify-between">
            <span className="text-sm">{plan.name}</span>
            <span className="text-sm font-medium">${plan.priceUsd}.00</span>
          </div>
          <div className="flex items-baseline justify-between text-xs text-[color:var(--muted)]">
            <span>VAT (5%, UAE)</span>
            <span>${(plan.priceUsd * 0.05).toFixed(2)}</span>
          </div>
          <div className="border-t border-[color:var(--border)] pt-2 flex items-baseline justify-between">
            <span className="text-sm font-semibold">Total</span>
            <span className="text-base font-semibold">${(plan.priceUsd * 1.05).toFixed(2)}</span>
          </div>
        </div>
        <p className="mt-4 text-xs text-[color:var(--muted)]">Renews automatically each {plan.interval}. Cancel anytime from your dashboard.</p>
      </div>
    </div>
  );
}
```

- [ ] **Step 6: Create `screens/Dashboard.tsx`**

Write to `src/demos/taxai-wizard/screens/Dashboard.tsx`:

```tsx
// src/demos/taxai-wizard/screens/Dashboard.tsx
//
// Post-payment dashboard: quota widget (messages used / total), subscription
// status card, and a "Next steps" hint.

import { Activity, Calendar, MessageSquare } from "lucide-react";
import { useTheme } from "@/demos/_shared/useTheme";
import { StatTile } from "@/demos/_shared/StatTile";
import { Badge } from "@/demos/_shared/Badge";
import { PLANS, SAMPLE_SUBSCRIPTION, SAMPLE_USER } from "../mocks";

export function Dashboard() {
  useTheme("taxai-wizard");
  const plan = PLANS.find((p) => p.id === SAMPLE_SUBSCRIPTION.planId)!;
  const remaining = plan.messageQuota - SAMPLE_SUBSCRIPTION.messagesUsed;
  const pct = Math.round((SAMPLE_SUBSCRIPTION.messagesUsed / plan.messageQuota) * 100);

  return (
    <div className="mx-auto max-w-5xl px-6 py-10">
      <div className="flex items-center justify-between">
        <div>
          <p className="text-xs font-semibold uppercase tracking-wider text-[color:var(--muted)]">Welcome back</p>
          <h2 className="mt-0.5 text-2xl font-semibold tracking-tight">{SAMPLE_USER.name}</h2>
        </div>
        <Badge tone="ok">Active · {plan.name}</Badge>
      </div>

      <div className="mt-6 grid gap-4 sm:grid-cols-3">
        <StatTile label="Messages used" value={`${SAMPLE_SUBSCRIPTION.messagesUsed} / ${plan.messageQuota}`} icon={<MessageSquare className="h-4 w-4" />} />
        <StatTile label="Remaining" value={remaining.toLocaleString()} icon={<Activity className="h-4 w-4" />} />
        <StatTile label="Renews on" value={SAMPLE_SUBSCRIPTION.expiresAt} icon={<Calendar className="h-4 w-4" />} />
      </div>

      <div className="mt-6 rounded-lg border border-[color:var(--border)] bg-[color:var(--surface)] p-6">
        <h3 className="text-sm font-semibold">Usage this quarter</h3>
        <div className="mt-3 h-2 w-full overflow-hidden rounded-full bg-[color:var(--border)]">
          <div className="h-full rounded-full bg-[color:var(--accent)] transition-all" style={{ width: `${pct}%` }} />
        </div>
        <p className="mt-2 text-xs text-[color:var(--muted)]">{pct}% of your {plan.messageQuota}-message quota used.</p>
      </div>

      <div className="mt-6 rounded-lg border border-[color:var(--border)] bg-[color:var(--surface)] p-6">
        <h3 className="text-sm font-semibold">Next steps</h3>
        <ul className="mt-3 space-y-2 text-sm">
          <li className="flex items-start gap-2">
            <span className="mt-0.5 h-1.5 w-1.5 shrink-0 rounded-full bg-[color:var(--accent)]" />
            Open <a className="font-medium underline" href="#/demos/taxai-chat">TaxAI Chat</a> to ask a tax question.
          </li>
          <li className="flex items-start gap-2">
            <span className="mt-0.5 h-1.5 w-1.5 shrink-0 rounded-full bg-[color:var(--accent)]" />
            Try <a className="font-medium underline" href="#/demos/taxai-talk">TaxAI Talk</a> for voice-based tax queries.
          </li>
        </ul>
      </div>
    </div>
  );
}
```

- [ ] **Step 7: Create `index.tsx`**

Write to `src/demos/taxai-wizard/index.tsx`:

```tsx
// src/demos/taxai-wizard/index.tsx
//
// Top-level shell for TaxAI Wizard. Wraps `Shell`, mounts a top nav between
// the 4 screens (no icon sidebar — wizard is linear flow), switches on `sub`.

import { Shell } from "@/demos/_shared/Shell";
import { useTheme } from "@/demos/_shared/useTheme";
import { setDemoHash } from "@/demos/router";
import type { DemoTheme } from "@/demos/_shared/theme";
import { TAXAI_WIZARD_SCREENS, getScreenLabel } from "./routes";
import { Register } from "./screens/Register";
import { Plans } from "./screens/Plans";
import { Checkout } from "./screens/Checkout";
import { Dashboard } from "./screens/Dashboard";
import { cn } from "@/lib/utils";

export function TaxaiWizard({ theme, sub }: { theme: DemoTheme; sub: string | null }) {
  useTheme(theme.id);
  const screen = getScreenLabel(sub, "register");

  const nav = (
    <ul className="flex items-center gap-1 px-6 py-2 border-b border-[color:var(--border)]">
      {TAXAI_WIZARD_SCREENS.map((s, i) => {
        const active = screen === s.id;
        return (
          <li key={s.id}>
            <button
              type="button"
              onClick={() => setDemoHash(theme.id, s.id)}
              className={cn(
                "inline-flex items-center gap-2 rounded-md px-3 py-1.5 text-xs font-medium transition-colors",
                active ? "bg-[color:var(--surface)] text-[color:var(--accent)]" : "text-[color:var(--muted)] hover:text-[color:var(--fg)]",
              )}
            >
              <span className="flex h-5 w-5 items-center justify-center rounded-full bg-[color:var(--border)] text-[10px] font-semibold">
                {i + 1}
              </span>
              {s.label}
            </button>
          </li>
        );
      })}
    </ul>
  );

  const content = (() => {
    switch (screen) {
      case "register": return <Register />;
      case "plans": return <Plans />;
      case "checkout": return <Checkout />;
      case "dashboard": return <Dashboard />;
    }
  })();

  return <Shell theme={theme} topNav={nav}>{content}</Shell>;
}
```

- [ ] **Step 8: Create `README.md`**

Write to `src/demos/taxai-wizard/README.md`:

```markdown
# TaxAI Wizard

This demo re-implements the **tax-ai-wizard-web-70** subscription onboarding
flow as a synthetic prototype. Four screens are wired: **Register** (default
— name, email, password, job title), **Plans** (four tier cards — Free Trial
$0, Monthly $99, Quarterly $250 highlighted, Yearly $899), **Checkout**
(mock Stripe Elements with a live-looking card form and order summary with
5% UAE VAT), and **Dashboard** (quota widget with usage bar, subscription
status, renew date, and "next steps" linking to the sibling TaxAI demos).

All names, emails, prices, and quota numbers are synthetic placeholders
(`Sara Al-Mansouri`, `sara.mansouri@example.ae`). UAE-specific touches:
5% VAT line, AED/USD pricing, `.ae` email domain. Brand identity and layout
follow shadcn new-york with teal accent (`#0EA5A4`) — no production code,
schemas, or fixtures committed here.
```

- [ ] **Step 9: Run typecheck**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
npm run typecheck 2>&1 | tail -20
```

Expected: taxai-wizard errors gone. taxai-chat and taxai-talk still failing.

- [ ] **Step 10: Commit**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
git add src/demos/taxai-wizard/
git commit -m "feat(demos): add TaxAI Wizard demo (register/plans/checkout/dashboard)"
```

---

## Task 7: Build TaxAI Chat

**Files:**
- Create: `src/demos/taxai-chat/routes.tsx`
- Create: `src/demos/taxai-chat/mocks.ts`
- Create: `src/demos/taxai-chat/screens/Inbox.tsx`
- Create: `src/demos/taxai-chat/screens/Conversation.tsx`
- Create: `src/demos/taxai-chat/screens/Settings.tsx`
- Create: `src/demos/taxai-chat/index.tsx`
- Create: `src/demos/taxai-chat/README.md`

- [ ] **Step 1: Create `routes.tsx`**

Write to `src/demos/taxai-chat/routes.tsx`:

```ts
// src/demos/taxai-chat/routes.tsx
//
// TaxAI Chat sub-routes: inbox (default) → conversation → settings.

export type TaxaiChatScreen = "inbox" | "conversation" | "settings";

export const TAXAI_CHAT_SCREENS: { id: TaxaiChatScreen; label: string }[] = [
  { id: "inbox", label: "Inbox" },
  { id: "conversation", label: "Conversation" },
  { id: "settings", label: "Settings" },
];

export function getScreenLabel(
  sub: string | null,
  fallback: TaxaiChatScreen,
): TaxaiChatScreen {
  if (!sub) return fallback;
  const found = TAXAI_CHAT_SCREENS.find((s) => s.id === sub);
  return (found?.id ?? fallback) as TaxaiChatScreen;
}
```

- [ ] **Step 2: Create `mocks.ts`**

Write to `src/demos/taxai-chat/mocks.ts`:

```ts
// src/demos/taxai-chat/mocks.ts
//
// Synthetic fixtures for TaxAI Chat: conversation list with UAE tax topics,
// sample message bubbles, token usage, and language settings.

export interface Conversation {
  id: string;
  title: string;
  topic: "VAT" | "Corporate Tax" | "Free Zones" | "Excise" | "Transfer Pricing";
  preview: string;
  updatedAt: string;
  messageCount: number;
  unread?: boolean;
}

export interface Message {
  id: string;
  conversationId: string;
  role: "user" | "assistant";
  content: string;
  attachmentName?: string;
  citations?: { source: string; snippet: string }[];
  timestamp: string;
}

export const CONVERSATIONS: Conversation[] = [
  { id: "c-001", title: "VAT rate on restaurant food", topic: "VAT", preview: "The standard VAT rate in the UAE is 5%...", updatedAt: "2026-08-13", messageCount: 8, unread: true },
  { id: "c-002", title: "Corporate tax registration threshold", topic: "Corporate Tax", preview: "Businesses with taxable income above AED 375,000...", updatedAt: "2026-08-12", messageCount: 14 },
  { id: "c-003", title: "Free zone qualifying income", topic: "Free Zones", preview: "Qualifying Free Zone Persons can benefit from 0%...", updatedAt: "2026-08-11", messageCount: 22 },
  { id: "c-004", title: "Excise tax on soft drinks", topic: "Excise", preview: "Excise tax is 50% on carbonated drinks...", updatedAt: "2026-08-10", messageCount: 6 },
  { id: "c-005", title: "Transfer pricing documentation", topic: "Transfer Pricing", preview: "Maintain a Master File, Local File, and CbCR...", updatedAt: "2026-08-08", messageCount: 19 },
  { id: "c-006", title: "VAT on imported services", topic: "VAT", preview: "Reverse charge mechanism applies when...", updatedAt: "2026-08-05", messageCount: 11 },
];

export const SAMPLE_MESSAGES: Message[] = [
  {
    id: "m-1", conversationId: "c-001", role: "user",
    content: "What's the VAT rate for restaurants in the UAE? And does it differ for dine-in vs takeaway?",
    timestamp: "10:42",
  },
  {
    id: "m-2", conversationId: "c-001", role: "assistant",
    content: "The standard VAT rate in the UAE is **5%**, and it applies uniformly to restaurant food and non-alcoholic beverages — there's no distinction between dine-in and takeaway in the Federal Decree-Law No. (8) of 2017.\n\nThat said, the Executive Regulations clarify that **catering services** (where the supplier provides preparation and delivery as a bundled service) follow the same 5% rate. If a restaurant only delivers prepared food without service, it's still 5%.",
    citations: [
      { source: "Federal Decree-Law No. (8) of 2017, Art. 2", snippet: "A supply of goods or services is considered to take place in the State..." },
      { source: "Executive Regulations, Art. 13", snippet: "Restaurant and catering services shall be considered as supplies of services..." },
    ],
    timestamp: "10:42",
  },
  {
    id: "m-3", conversationId: "c-001", role: "user",
    content: "Got it. And what about alcoholic beverages if a restaurant is licensed?",
    timestamp: "10:44",
  },
  {
    id: "m-4", conversationId: "c-001", role: "assistant",
    content: "Alcoholic beverages are subject to **excise tax at 50%** (in addition to the 5% VAT). The excise is levied at the point of import or release from an excise warehouse, so the restaurant would purchase them excise-paid from the importer.",
    attachmentName: "excise-tax-schedule-2024.pdf",
    timestamp: "10:44",
  },
];

export const TOKEN_QUOTA = { used: 18_420, limit: 50_000 };
```

- [ ] **Step 3: Create `screens/Inbox.tsx`**

Write to `src/demos/taxai-chat/screens/Inbox.tsx`:

```tsx
// src/demos/taxai-chat/screens/Inbox.tsx
//
// Conversation list — sidebar-style with topic chip, unread badge, and a
// token usage footer. Clicking a row navigates to /conversation.

import { Search, MessageSquare } from "lucide-react";
import { useTheme } from "@/demos/_shared/useTheme";
import { Badge } from "@/demos/_shared/Badge";
import { setDemoHash } from "@/demos/router";
import { cn } from "@/lib/utils";
import { CONVERSATIONS, TOKEN_QUOTA } from "../mocks";

const TOPIC_TONE = {
  VAT: "ok",
  "Corporate Tax": "accent",
  "Free Zones": "neutral",
  Excise: "warn",
  "Transfer Pricing": "neutral",
} as const;

export function Inbox() {
  useTheme("taxai-chat");
  const pct = Math.round((TOKEN_QUOTA.used / TOKEN_QUOTA.limit) * 100);

  return (
    <div className="grid h-full grid-cols-[320px_1fr]">
      <aside className="flex h-full flex-col border-r border-[color:var(--border)] bg-[color:var(--surface)]">
        <div className="border-b border-[color:var(--border)] p-3">
          <div className="flex items-center gap-2 rounded-md border border-[color:var(--border)] bg-white px-3 py-1.5 text-sm">
            <Search className="h-3.5 w-3.5 text-[color:var(--muted)]" />
            <input
              className="flex-1 bg-transparent outline-none placeholder:text-[color:var(--muted)]"
              placeholder="Search conversations..."
            />
          </div>
        </div>

        <ul className="flex-1 overflow-y-auto">
          {CONVERSATIONS.map((c) => (
            <li key={c.id}>
              <button
                type="button"
                onClick={() => setDemoHash("taxai-chat", "conversation")}
                className={cn(
                  "flex w-full flex-col gap-1 border-b border-[color:var(--border)] px-3 py-2.5 text-left hover:bg-white",
                  c.unread && "bg-white",
                )}
              >
                <div className="flex items-center justify-between gap-2">
                  <span className="truncate text-sm font-medium">{c.title}</span>
                  {c.unread && <span className="h-1.5 w-1.5 shrink-0 rounded-full bg-[color:var(--accent)]" />}
                </div>
                <p className="truncate text-xs text-[color:var(--muted)]">{c.preview}</p>
                <div className="flex items-center justify-between gap-2 text-[10px]">
                  <Badge tone={TOPIC_TONE[c.topic]}>{c.topic}</Badge>
                  <span className="text-[color:var(--muted)]">{c.updatedAt}</span>
                </div>
              </button>
            </li>
          ))}
        </ul>

        <div className="border-t border-[color:var(--border)] p-3">
          <div className="flex items-baseline justify-between text-xs">
            <span className="text-[color:var(--muted)]">Tokens this month</span>
            <span className="font-medium">{TOKEN_QUOTA.used.toLocaleString()} / {TOKEN_QUOTA.limit.toLocaleString()}</span>
          </div>
          <div className="mt-2 h-1.5 w-full overflow-hidden rounded-full bg-[color:var(--border)]">
            <div className="h-full rounded-full bg-[color:var(--accent)]" style={{ width: `${pct}%` }} />
          </div>
        </div>
      </aside>

      <main className="flex items-center justify-center text-[color:var(--muted)]">
        <div className="text-center">
          <MessageSquare className="mx-auto h-8 w-8" />
          <p className="mt-3 text-sm">Select a conversation to continue.</p>
        </div>
      </main>
    </div>
  );
}
```

- [ ] **Step 4: Create `screens/Conversation.tsx`**

Write to `src/demos/taxai-chat/screens/Conversation.tsx`:

```tsx
// src/demos/taxai-chat/screens/Conversation.tsx
//
// Conversation view — dual bubble (user/assistant), citation cards under AI
// messages, attachment chips, and a composer with paperclip + send button.

import { useState } from "react";
import { Paperclip, Send, FileText, ExternalLink, ChevronLeft } from "lucide-react";
import { useTheme } from "@/demos/_shared/useTheme";
import { setDemoHash } from "@/demos/router";
import { SAMPLE_MESSAGES } from "../mocks";

export function Conversation() {
  useTheme("taxai-chat");
  const [draft, setDraft] = useState("");

  return (
    <div className="flex h-full flex-col">
      <header className="flex items-center gap-2 border-b border-[color:var(--border)] bg-[color:var(--surface)] px-4 py-2.5">
        <button onClick={() => setDemoHash("taxai-chat", "inbox")} className="text-[color:var(--muted)] hover:text-[color:var(--fg)]">
          <ChevronLeft className="h-4 w-4" />
        </button>
        <div>
          <p className="text-sm font-semibold">VAT rate on restaurant food</p>
          <p className="text-[10px] text-[color:var(--muted)]">VAT · 8 messages · GPT-4o · today</p>
        </div>
      </header>

      <div className="flex-1 overflow-y-auto px-4 py-6 space-y-4">
        {SAMPLE_MESSAGES.map((m) =>
          m.role === "user" ? (
            <div key={m.id} className="flex justify-end">
              <div className="max-w-[75%] rounded-2xl rounded-tr-sm bg-[color:var(--accent)] px-4 py-2.5 text-sm text-[color:var(--accent-fg)]">
                {m.content}
                <div className="mt-1 text-[10px] opacity-70">{m.timestamp}</div>
              </div>
            </div>
          ) : (
            <div key={m.id} className="flex flex-col items-start gap-2">
              <div className="max-w-[80%] rounded-2xl rounded-tl-sm bg-[color:var(--surface)] px-4 py-2.5 text-sm border border-[color:var(--border)]">
                <p className="whitespace-pre-wrap">{m.content}</p>

                {m.attachmentName && (
                  <div className="mt-2 inline-flex items-center gap-2 rounded-md border border-[color:var(--border)] bg-white px-2 py-1 text-xs">
                    <FileText className="h-3 w-3 text-[color:var(--accent)]" /> {m.attachmentName}
                  </div>
                )}

                {m.citations && (
                  <div className="mt-3 space-y-1.5">
                    {m.citations.map((c) => (
                      <div key={c.source} className="rounded-md border border-[color:var(--border)] bg-white px-2.5 py-1.5 text-xs">
                        <p className="flex items-center gap-1 font-medium text-[color:var(--accent)]">
                          <ExternalLink className="h-3 w-3" /> {c.source}
                        </p>
                        <p className="mt-0.5 text-[color:var(--muted)]">"{c.snippet}"</p>
                      </div>
                    ))}
                  </div>
                )}

                <div className="mt-1 text-[10px] text-[color:var(--muted)]">{m.timestamp}</div>
              </div>
            </div>
          ),
        )}
      </div>

      <footer className="border-t border-[color:var(--border)] bg-white px-4 py-3">
        <div className="flex items-end gap-2 rounded-lg border border-[color:var(--border)] bg-[color:var(--surface)] px-3 py-2">
          <button className="text-[color:var(--muted)] hover:text-[color:var(--fg)]" aria-label="Attach file">
            <Paperclip className="h-4 w-4" />
          </button>
          <textarea
            value={draft}
            onChange={(e) => setDraft(e.target.value)}
            placeholder="Ask about UAE tax..."
            className="flex-1 resize-none bg-transparent text-sm outline-none placeholder:text-[color:var(--muted)]"
            rows={1}
          />
          <button
            type="button"
            className="flex h-7 w-7 items-center justify-center rounded-md bg-[color:var(--accent)] text-[color:var(--accent-fg)] disabled:opacity-50"
            disabled={!draft.trim()}
            aria-label="Send"
          >
            <Send className="h-3.5 w-3.5" />
          </button>
        </div>
      </footer>
    </div>
  );
}
```

- [ ] **Step 5: Create `screens/Settings.tsx`**

Write to `src/demos/taxai-chat/screens/Settings.tsx`:

```tsx
// src/demos/taxai-chat/screens/Settings.tsx
//
// Settings page — language detection toggle (EN/AR/auto), model picker
// display, and account info. Visual only, no save.

import { Globe, Cpu, User } from "lucide-react";
import { useTheme } from "@/demos/_shared/useTheme";
import { Field } from "@/demos/_shared/Field";
import { SAMPLE_USER } from "../mocks";

export function Settings() {
  useTheme("taxai-chat");
  return (
    <div className="mx-auto max-w-2xl px-6 py-10 space-y-8">
      <section>
        <h3 className="flex items-center gap-2 text-sm font-semibold">
          <Globe className="h-4 w-4" /> Language
        </h3>
        <p className="mt-1 text-xs text-[color:var(--muted)]">ChatGPT auto-detects your input language. You can force a specific response language.</p>
        <div className="mt-4">
          <Field label="Response language" value="Auto-detect" onChange={() => {}} placeholder="" />
        </div>
        <div className="mt-3 flex gap-2 text-xs">
          <span className="rounded-full bg-[color:var(--accent)] px-3 py-1 font-medium text-[color:var(--accent-fg)]">Auto</span>
          <span className="rounded-full border border-[color:var(--border)] bg-[color:var(--surface)] px-3 py-1">English</span>
          <span className="rounded-full border border-[color:var(--border)] bg-[color:var(--surface)] px-3 py-1">العربية</span>
        </div>
      </section>

      <section>
        <h3 className="flex items-center gap-2 text-sm font-semibold">
          <Cpu className="h-4 w-4" /> Model
        </h3>
        <div className="mt-4">
          <Field label="Reasoning model" value="GPT-4o (default)" onChange={() => {}} placeholder="" />
        </div>
      </section>

      <section>
        <h3 className="flex items-center gap-2 text-sm font-semibold">
          <User className="h-4 w-4" /> Account
        </h3>
        <div className="mt-4 grid grid-cols-2 gap-3">
          <Field label="Name" value={SAMPLE_USER.name} onChange={() => {}} placeholder="" />
          <Field label="Email" value="sara.mansouri@example.ae" onChange={() => {}} placeholder="" />
        </div>
      </section>
    </div>
  );
}
```

- [ ] **Step 6: Create `index.tsx`**

Write to `src/demos/taxai-chat/index.tsx`:

```tsx
// src/demos/taxai-chat/index.tsx
//
// Top-level shell for TaxAI Chat. Wraps Shell with a thin top nav between 3
// screens, switches on `sub`.

import { Shell } from "@/demos/_shared/Shell";
import { useTheme } from "@/demos/_shared/useTheme";
import { setDemoHash } from "@/demos/router";
import type { DemoTheme } from "@/demos/_shared/theme";
import { TAXAI_CHAT_SCREENS, getScreenLabel } from "./routes";
import { Inbox } from "./screens/Inbox";
import { Conversation } from "./screens/Conversation";
import { Settings } from "./screens/Settings";
import { cn } from "@/lib/utils";

export function TaxaiChat({ theme, sub }: { theme: DemoTheme; sub: string | null }) {
  useTheme(theme.id);
  const screen = getScreenLabel(sub, "inbox");

  const nav = (
    <ul className="flex items-center gap-1 border-b border-[color:var(--border)] px-6 py-2">
      {TAXAI_CHAT_SCREENS.map((s) => {
        const active = screen === s.id;
        return (
          <li key={s.id}>
            <button
              type="button"
              onClick={() => setDemoHash(theme.id, s.id)}
              className={cn(
                "rounded-md px-3 py-1.5 text-xs font-medium transition-colors",
                active ? "bg-[color:var(--surface)] text-[color:var(--accent)]" : "text-[color:var(--muted)] hover:text-[color:var(--fg)]",
              )}
            >
              {s.label}
            </button>
          </li>
        );
      })}
    </ul>
  );

  const content = (() => {
    switch (screen) {
      case "inbox": return <Inbox />;
      case "conversation": return <Conversation />;
      case "settings": return <Settings />;
    }
  })();

  return <Shell theme={theme} topNav={nav}>{content}</Shell>;
}
```

- [ ] **Step 7: Create `README.md`**

Write to `src/demos/taxai-chat/README.md`:

```markdown
# TaxAI Chat

This demo re-implements the **chat.taxai** chat-style tax Q&A interface as
a synthetic prototype. Three screens are wired: **Inbox** (default — left
sidebar with 6 sample conversations tagged by topic: VAT, Corporate Tax, Free
Zones, Excise, Transfer Pricing, with unread dots, a search field, and a
token quota footer at 36%), **Conversation** (dual bubble layout — user on
the right in accent color, AI on the left in a bordered card — with citation
cards under AI answers quoting "Federal Decree-Law No. (8) of 2017" and the
Executive Regulations, plus a PDF attachment chip), and **Settings** (language
detection toggle showing Auto/EN/AR pills, model picker, account info).

All conversations, citation snippets, and attachment names are synthetic
placeholders. UAE-specific touches: VAT at 5%, AED 375,000 corporate tax
threshold, Free Zone qualifying income rules. Brand identity and layout
follow shadcn new-york with indigo accent (`#4F46E5`) — no production code,
schemas, or fixtures committed here.
```

- [ ] **Step 8: Run typecheck**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
npm run typecheck 2>&1 | tail -20
```

Expected: taxai-chat errors gone. taxai-talk still failing.

- [ ] **Step 9: Commit**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
git add src/demos/taxai-chat/
git commit -m "feat(demos): add TaxAI Chat demo (inbox/conversation/settings)"
```

---

## Task 8: Build TaxAI Talk

**Files:**
- Create: `src/demos/taxai-talk/routes.tsx`
- Create: `src/demos/taxai-talk/mocks.ts`
- Create: `src/demos/taxai-talk/screens/VoiceSession.tsx`
- Create: `src/demos/taxai-talk/screens/Transcript.tsx`
- Create: `src/demos/taxai-talk/screens/Settings.tsx`
- Create: `src/demos/taxai-talk/index.tsx`
- Create: `src/demos/taxai-talk/README.md`

- [ ] **Step 1: Create `routes.tsx`**

Write to `src/demos/taxai-talk/routes.tsx`:

```ts
// src/demos/taxai-talk/routes.tsx
//
// TaxAI Talk sub-routes: voice (default) → transcript → settings.

export type TaxaiTalkScreen = "voice" | "transcript" | "settings";

export const TAXAI_TALK_SCREENS: { id: TaxaiTalkScreen; label: string }[] = [
  { id: "voice", label: "Voice" },
  { id: "transcript", label: "Transcript" },
  { id: "settings", label: "Settings" },
];

export function getScreenLabel(
  sub: string | null,
  fallback: TaxaiTalkScreen,
): TaxaiTalkScreen {
  if (!sub) return fallback;
  const found = TAXAI_TALK_SCREENS.find((s) => s.id === sub);
  return (found?.id ?? fallback) as TaxaiTalkScreen;
}
```

- [ ] **Step 2: Create `mocks.ts`**

Write to `src/demos/taxai-talk/mocks.ts`:

```ts
// src/demos/taxai-talk/mocks.ts
//
// Synthetic fixtures for TaxAI Talk. Refit to tax context per spec: the
// talk.taxai.ae production is a generic voice assistant, but the portfolio
// re-frames it to a UAE tax voice Q&A demo. Voices are ElevenLabs-style
// names from public knowledge.

export type VoiceId = "aria" | "river" | "sarah" | "george";

export interface Voice {
  id: VoiceId;
  name: string;
  language: "EN" | "AR" | "Multilingual";
  description: string;
}

export interface TranscriptTurn {
  id: string;
  role: "user" | "assistant";
  language: "EN" | "AR";
  content: string;
  audioUrl?: string;
  timestamp: string;
}

export const VOICES: Voice[] = [
  { id: "aria", name: "Aria", language: "Multilingual", description: "Warm, clear, balanced — default ElevenLabs voice." },
  { id: "river", name: "River", language: "EN", description: "Calm, conversational, low pitch." },
  { id: "sarah", name: "Sarah", language: "EN", description: "Professional, news-anchor style." },
  { id: "george", name: "George", language: "EN", description: "Deep, authoritative, formal." },
];

export const SAMPLE_TRANSCRIPT: TranscriptTurn[] = [
  { id: "t-1", role: "user", language: "EN", content: "What's the VAT rate for restaurants in the UAE?", timestamp: "00:00" },
  { id: "t-2", role: "assistant", language: "EN", content: "The standard VAT rate in the UAE is 5%, applicable to restaurant food and non-alcoholic beverages. There is no distinction between dine-in and takeaway in the Federal Decree-Law.", timestamp: "00:03" },
  { id: "t-3", role: "user", language: "AR", content: "وماذا عن الضريبة الانتقائية على المشروبات الغازية؟", timestamp: "00:14" },
  { id: "t-4", role: "assistant", language: "AR", content: "الضريبة الانتقائية على المشروبات الغازية 50 في المئة، وعلى منتجات التبغ 100 في المئة، وعلى مشروبات الطاقة 100 في المئة أيضاً. تُفرض هذه الضريبة في نقطة الاستيراد أو الإفراج من المستودع الخاضع للضريبة الانتقائية.", timestamp: "00:17" },
  { id: "t-5", role: "user", language: "EN", content: "Thanks. Can you also tell me about corporate tax registration?", timestamp: "00:34" },
  { id: "t-6", role: "assistant", language: "EN", content: "Corporate tax registration with the Federal Tax Authority is mandatory for UAE resident juridical persons, and for non-resident persons with a permanent establishment in the UAE. The registration window depends on when your business was established.", timestamp: "00:37" },
];

export const SELECTED_VOICE: VoiceId = "aria";
```

- [ ] **Step 3: Create `screens/VoiceSession.tsx`**

Write to `src/demos/taxai-talk/screens/VoiceSession.tsx`:

```tsx
// src/demos/taxai-talk/screens/VoiceSession.tsx
//
// Live voice session view — centered waveform animation, mic/end-call
// controls, language indicator. SVG waveform is a static SVG with animated
// bars via Tailwind keyframes.

import { Mic, PhoneOff, Volume2 } from "lucide-react";
import { useTheme } from "@/demos/_shared/useTheme";
import { Button } from "@/demos/_shared/Button";
import { cn } from "@/lib/utils";
import { VOICES, SELECTED_VOICE } from "../mocks";

export function VoiceSession() {
  useTheme("taxai-talk");
  const voice = VOICES.find((v) => v.id === SELECTED_VOICE)!;

  return (
    <div className="flex h-full flex-col items-center justify-center px-6">
      <div className="text-center">
        <p className="text-xs font-semibold uppercase tracking-[0.3em] text-[color:var(--accent)]">Live session</p>
        <h2 className="mt-2 text-2xl font-semibold tracking-tight">Ask the tax code</h2>
        <p className="mt-1 text-sm text-[color:var(--muted)]">{voice.name} · {voice.language} · {voice.description}</p>
      </div>

      {/* Waveform (static SVG with animated bars) */}
      <div className="mt-10 flex items-center justify-center gap-1.5 h-24">
        {Array.from({ length: 24 }).map((_, i) => (
          <span
            key={i}
            className={cn("w-1 rounded-full bg-[color:var(--accent)]", "animate-pulse")}
            style={{
              height: `${20 + Math.abs(Math.sin(i * 0.6)) * 60}%`,
              animationDelay: `${i * 60}ms`,
              animationDuration: `${900 + (i % 4) * 150}ms`,
            }}
          />
        ))}
      </div>

      {/* Controls */}
      <div className="mt-10 flex items-center gap-4">
        <button
          className="flex h-12 w-12 items-center justify-center rounded-full border border-[color:var(--border)] bg-[color:var(--surface)] text-[color:var(--muted)] hover:text-[color:var(--fg)]"
          aria-label="Mute"
        >
          <Volume2 className="h-5 w-5" />
        </button>

        <button
          className="flex h-16 w-16 items-center justify-center rounded-full bg-[color:var(--accent)] text-white shadow-lg"
          aria-label="Speak"
        >
          <Mic className="h-7 w-7" />
        </button>

        <Button
          type="button"
          variant="secondary"
          className="h-12 w-12 !p-0 rounded-full"
          aria-label="End call"
        >
          <PhoneOff className="h-5 w-5" />
        </Button>
      </div>

      <p className="mt-6 text-xs text-[color:var(--muted)]">
        Powered by <span className="font-semibold text-[color:var(--accent)]">ElevenLabs</span> · GPT-4o reasoning
      </p>
    </div>
  );
}
```

- [ ] **Step 4: Create `screens/Transcript.tsx`**

Write to `src/demos/taxai-talk/screens/Transcript.tsx`:

```tsx
// src/demos/taxai-talk/screens/Transcript.tsx
//
// Transcript view — bilingual (EN/AR) sample conversation with timestamps
// and language pill per turn. Refit to UAE tax context per spec.

import { Play } from "lucide-react";
import { useTheme } from "@/demos/_shared/useTheme";
import { Badge } from "@/demos/_shared/Badge";
import { cn } from "@/lib/utils";
import { SAMPLE_TRANSCRIPT } from "../mocks";

export function Transcript() {
  useTheme("taxai-talk");
  return (
    <div className="mx-auto max-w-3xl px-6 py-10">
      <h2 className="text-xl font-semibold tracking-tight">Sample conversation</h2>
      <p className="mt-1 text-xs text-[color:var(--muted)]">UAE tax Q&A · multilingual voice · GPT-4o + ElevenLabs Aria</p>

      <ol className="mt-8 space-y-4">
        {SAMPLE_TRANSCRIPT.map((t) => (
          <li
            key={t.id}
            className={cn(
              "rounded-lg border p-4",
              t.role === "user"
                ? "border-[color:var(--border)] bg-[color:var(--surface)]"
                : "border-[color:var(--accent)]/30 bg-[color:var(--accent)]/5",
            )}
            dir={t.language === "AR" ? "rtl" : "ltr"}
          >
            <div className="flex items-center justify-between gap-3">
              <div className="flex items-center gap-2">
                <Badge tone={t.role === "user" ? "neutral" : "accent"}>
                  {t.role === "user" ? "You" : "TaxAI"}
                </Badge>
                <Badge tone="neutral">{t.language}</Badge>
              </div>
              <span className="text-[10px] text-[color:var(--muted)]">{t.timestamp}</span>
            </div>
            <p className={cn("mt-2 text-sm leading-relaxed", t.language === "AR" && "font-arabic")}>{t.content}</p>
            {t.role === "assistant" && (
              <button className="mt-3 inline-flex items-center gap-1.5 text-xs text-[color:var(--accent)] hover:underline">
                <Play className="h-3 w-3" /> Play audio
              </button>
            )}
          </li>
        ))}
      </ol>
    </div>
  );
}
```

- [ ] **Step 5: Create `screens/Settings.tsx`**

Write to `src/demos/taxai-talk/screens/Settings.tsx`:

```tsx
// src/demos/taxai-talk/screens/Settings.tsx
//
// Settings — voice selection cards (4 ElevenLabs voices) + response language
// picker. Visual only.

import { Check } from "lucide-react";
import { useTheme } from "@/demos/_shared/useTheme";
import { cn } from "@/lib/utils";
import { VOICES, SELECTED_VOICE } from "../mocks";

export function Settings() {
  useTheme("taxai-talk");

  return (
    <div className="mx-auto max-w-2xl px-6 py-10 space-y-8">
      <section>
        <h3 className="text-sm font-semibold">Voice</h3>
        <p className="mt-1 text-xs text-[color:var(--muted)]">ElevenLabs voices available for the assistant. Currently selected voice is highlighted.</p>

        <div className="mt-4 grid gap-3 sm:grid-cols-2">
          {VOICES.map((v) => {
            const active = v.id === SELECTED_VOICE;
            return (
              <button
                key={v.id}
                type="button"
                className={cn(
                  "relative rounded-lg border p-4 text-left transition-colors",
                  active ? "border-[color:var(--accent)] bg-[color:var(--surface)]" : "border-[color:var(--border)] hover:bg-[color:var(--surface)]",
                )}
              >
                {active && <Check className="absolute right-3 top-3 h-4 w-4 text-[color:var(--accent)]" />}
                <p className="text-sm font-semibold">{v.name}</p>
                <p className="mt-1 text-xs text-[color:var(--muted)]">{v.description}</p>
                <p className="mt-2 text-[10px] uppercase tracking-wider text-[color:var(--muted)]">{v.language}</p>
              </button>
            );
          })}
        </div>
      </section>

      <section>
        <h3 className="text-sm font-semibold">Response language</h3>
        <p className="mt-1 text-xs text-[color:var(--muted)]">Voice output follows this preference. Input is auto-detected.</p>
        <div className="mt-4 flex gap-2 text-xs">
          <span className="rounded-full bg-[color:var(--accent)] px-3 py-1 font-medium text-[color:var(--accent-fg)]">Multilingual</span>
          <span className="rounded-full border border-[color:var(--border)] bg-[color:var(--surface)] px-3 py-1">English</span>
          <span className="rounded-full border border-[color:var(--border)] bg-[color:var(--surface)] px-3 py-1">العربية</span>
        </div>
      </section>
    </div>
  );
}
```

- [ ] **Step 6: Create `index.tsx`**

Write to `src/demos/taxai-talk/index.tsx`:

```tsx
// src/demos/taxai-talk/index.tsx
//
// Top-level shell for TaxAI Talk. Wraps Shell with thin top nav between 3
// screens, switches on `sub`. Surface is dark (theme tokens).

import { Shell } from "@/demos/_shared/Shell";
import { useTheme } from "@/demos/_shared/useTheme";
import { setDemoHash } from "@/demos/router";
import type { DemoTheme } from "@/demos/_shared/theme";
import { TAXAI_TALK_SCREENS, getScreenLabel } from "./routes";
import { VoiceSession } from "./screens/VoiceSession";
import { Transcript } from "./screens/Transcript";
import { Settings } from "./screens/Settings";
import { cn } from "@/lib/utils";

export function TaxaiTalk({ theme, sub }: { theme: DemoTheme; sub: string | null }) {
  useTheme(theme.id);
  const screen = getScreenLabel(sub, "voice");

  const nav = (
    <ul className="flex items-center gap-1 border-b border-[color:var(--border)] px-6 py-2">
      {TAXAI_TALK_SCREENS.map((s) => {
        const active = screen === s.id;
        return (
          <li key={s.id}>
            <button
              type="button"
              onClick={() => setDemoHash(theme.id, s.id)}
              className={cn(
                "rounded-md px-3 py-1.5 text-xs font-medium transition-colors",
                active ? "bg-[color:var(--surface)] text-[color:var(--accent)]" : "text-[color:var(--muted)] hover:text-[color:var(--fg)]",
              )}
            >
              {s.label}
            </button>
          </li>
        );
      })}
    </ul>
  );

  const content = (() => {
    switch (screen) {
      case "voice": return <VoiceSession />;
      case "transcript": return <Transcript />;
      case "settings": return <Settings />;
    }
  })();

  return <Shell theme={theme} topNav={nav}>{content}</Shell>;
}
```

- [ ] **Step 7: Create `README.md`**

Write to `src/demos/taxai-talk/README.md`:

```markdown
# TaxAI Talk

This demo re-implements **talk.taxai.ae** as a synthetic prototype, **refit
to a UAE tax voice Q&A context** per spec. (The production repo is a
generic voice assistant, but for portfolio narrative coherence the demo
shows tax-themed sample conversations.) Three screens are wired:
**Voice** (default — centered waveform animation with 24 staggered pulsing
bars, three controls: mute / mic (large accent button) / end-call, footer
crediting ElevenLabs + GPT-4o), **Transcript** (bilingual sample
conversation alternating EN/AR with `dir="rtl"` on Arabic turns — covering
VAT rate, excise tax, and corporate tax registration), **Settings** (4
ElevenLabs voice cards: Aria / River / Sarah / George, with the selected
voice highlighted, plus language preference pills).

All voices are public knowledge of ElevenLabs's catalog. The transcript
content is synthetic and intentionally topical to UAE tax. Brand identity
follows shadcn new-york with a dark surface (`#0B0B14` bg) and violet
accent (`#7C3AED`) — no production code, schemas, or fixtures committed
here.
```

- [ ] **Step 8: Run typecheck**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
npm run typecheck 2>&1 | tail -20
```

Expected: clean — no errors. All 4 demos are now wired.

- [ ] **Step 9: Run build**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
npm run build 2>&1 | tail -30
```

Expected: builds without errors.

- [ ] **Step 10: Commit**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
git add src/demos/taxai-talk/
git commit -m "feat(demos): add TaxAI Talk demo (voice/transcript/settings)"
```

---

## Task 9: Hub verification

**Files:** none modified.

- [ ] **Step 1: Start dev server in background**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
npm run dev > /tmp/dev.log 2>&1 &
echo $! > /tmp/dev.pid
sleep 5
curl -fsS http://localhost:5173 > /dev/null && echo "dev server OK" || echo "dev server failed"
```

Expected: "dev server OK" after 5 seconds.

- [ ] **Step 2: Open DemoHub and verify 9 cards**

Use Playwright:

1. Navigate to `http://localhost:5173/#/demos`
2. Take snapshot — confirm 9 demo cards visible
3. Verify titles: Invenflow, Invoice Sense, Channelflow, Kitchen Fresh, People & Culture, Laguku, TaxAI Wizard, TaxAI Chat, TaxAI Talk

- [ ] **Step 3: Verify each new demo loads**

For each of `#/demos/laguku`, `#/demos/taxai-wizard`, `#/demos/taxai-chat`, `#/demos/taxai-talk`:

1. Navigate to hash
2. Wait for shell to render
3. Verify brand tile + theme accent color visible
4. For iframe (laguku): verify `<iframe>` element present with src `https://laguku.co`
5. For TaxAI Wizard: click Plans, Checkout, Dashboard — each screen renders
6. For TaxAI Chat: click Inbox (default), Conversation, Settings — each renders
7. For TaxAI Talk: click Voice (default), Transcript, Settings — each renders

- [ ] **Step 4: Stop dev server**

```bash
kill $(cat /tmp/dev.pid) 2>/dev/null
rm -f /tmp/dev.pid /tmp/dev.log
```

- [ ] **Step 5: Final commit (if any uncommitted changes from verification)**

```bash
cd /Users/yanuar/Documents/abdurrahmanfirdaus.com
git status --short
git add -A
git commit -m "chore(demos): final verification commit" --allow-empty
```

---

## Self-Review

**1. Spec coverage:**
- §3.1 (DemoId union extension) → Task 3 Step 2 ✓
- §3.2 (router imports + DEMO_COMPONENTS + MobileViewportNotice override) → Task 4 Step 2 ✓
- §3.3 (4 theme files) → Task 2 ✓
- §3.4 (Brand LOGOS) → Task 4 Step 1 ✓
- §3.5 (2 public assets) → Task 1 ✓
- §4.1 (Laguku iframe) → Task 5 ✓
- §4.2 (TaxAI Wizard, 4 screens) → Task 6 ✓
- §4.3 (TaxAI Chat, 3 screens) → Task 7 ✓
- §4.4 (TaxAI Talk, 3 screens, tax-refit) → Task 8 ✓
- §5.1 (DemoHub order via DEMOS array append) → Task 3 Step 2 ✓
- §5.2 (copywriting locked in DEMOS entries) → Task 3 Step 2 ✓
- §5.3 (AI stack reference in README) → Tasks 6-8 Step (README) ✓
- §9 verification → Task 9 ✓

**2. Placeholder scan:** No "TBD"/"TODO" in steps. All code blocks contain concrete TSX.

**3. Type consistency:**
- `DemoId` union defined once in Task 3, used in router + index.
- Theme file exports: `lagukuTheme`, `taxaiWizardTheme`, `taxaiChatTheme`, `taxaiTalkTheme` — consistent across imports and THEMES map.
- Component exports: `Laguku`, `TaxaiWizard`, `TaxaiChat`, `TaxaiTalk` — consistent in router and demo folders.
- `getScreenLabel(sub, fallback)` signature consistent across all 4 demos.
- Theme id strings `"laguku"`, `"taxai-wizard"`, etc. consistent.

**4. Open issues:** None — all spec decisions implemented.
